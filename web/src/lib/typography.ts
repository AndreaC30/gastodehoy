/** Legible text sizes (mobile-first, bumped for older users). */

/** Short uppercase labels (metrics, hero eyebrow). */
export const TYPE_EYEBROW =
  "text-xs font-semibold uppercase tracking-wide text-slate-400 sm:text-sm";

/** Secondary lines under headings. */
export const TYPE_CAPTION = "text-sm text-slate-400 sm:text-base";

/** Paragraphs and explanations. */
export const TYPE_BODY = "text-base leading-relaxed text-slate-400";

/** Form labels and hints. */
export const TYPE_LABEL = "text-sm font-medium text-slate-400";

export const TYPE_HINT = "text-sm leading-relaxed text-slate-400";

/** Small meta (still ≥12px). */
export const TYPE_META = "text-xs text-slate-500 sm:text-sm";
