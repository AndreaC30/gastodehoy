/** Tipografía alineada al mockup V11 (Inter body, Space Grotesk display, mono labels). */

/** Short uppercase labels (metrics, hero eyebrow). */
export const TYPE_EYEBROW =
  "font-mono text-[11px] font-medium uppercase tracking-[0.06em] text-[var(--color-text-dim)] sm:text-xs";

/** Secondary lines under headings (never title-case every word). */
export const TYPE_CAPTION =
  "text-sm text-[var(--color-text-muted)] sm:text-base normal-case";

/** Paragraphs and explanations. */
export const TYPE_BODY =
  "text-sm leading-relaxed text-[var(--color-text-muted)] sm:text-base";

/** Form labels and hints. */
export const TYPE_LABEL =
  "text-sm font-medium text-[var(--color-text-muted)]";

export const TYPE_HINT =
  "text-sm leading-relaxed text-[var(--color-text-muted)]";

/** Small meta (still ≥12px). */
export const TYPE_META =
  "font-mono text-xs text-[var(--color-text-dim)] sm:text-sm";

/** Hero numbers — display + accent (importe principal). */
export const TYPE_HERO_NUMBER =
  "font-display text-4xl font-semibold tabular-nums tracking-tight text-[var(--color-accent)] sm:text-4xl md:text-[2.75rem]";

/** Display headings — Space Grotesk para títulos principales. */
export const TYPE_DISPLAY =
  "font-display text-xl font-semibold tracking-tight text-[var(--color-text)] sm:text-2xl md:text-[1.35rem]";
