# Local dev: expose API on host :8001 (matches VITE_API_PROXY_TARGET in .env).
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
#   cd web && npm run dev
services:
  app:
    ports:
      - "8001:8000"
    environment:
      ENV: development
